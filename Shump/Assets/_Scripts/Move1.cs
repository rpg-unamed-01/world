using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Move1 : Enemy
{
    // Start is called before the first frame update
    private void Start()
    {
        enemy = this.gameObject;
    }
}
